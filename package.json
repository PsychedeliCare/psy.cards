{
  "name": "psy.cards",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cloudflare": "WRANGLER_LOG_PATH=$PWD/.wrangler/logs wrangler pages dev $PWD/dist --cwd ${TMPDIR:-/tmp} --port 8788 --compatibility-date 2026-04-23 --show-interactive-dev-session false",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.0",
    "@astrojs/mdx": "^5.0.4",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.1.9",
    "tailwindcss": "^4.2.4",
    "wrangler": "^4.84.1"
  },
  "overrides": {
    "vite": "^7"
  }
}
