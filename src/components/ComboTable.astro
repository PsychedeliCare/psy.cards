---
import CategoryNav from "./CategoryNav.astro";
import ComboCell from "./ComboCell.astro";
import { columns, commonSubstanceKeySet } from "../data/config";

interface Props {
  comboRoute?: string;
  initialSubstanceSet?: "common" | "full";
}

const { comboRoute = "/combos", initialSubstanceSet = "full" } = Astro.props;

const isCommonSubstance = (key: string) => commonSubstanceKeySet.has(key);

function getLabelSizeClass(label: string) {
  if (label.length >= 11) return "label-xs";
  if (label.length >= 8) return "label-sm";
  return "label-md";
}
---

<section
  class="table-wrap"
  aria-label="Drug combinations"
  data-medication="true"
  data-hover-enabled="true"
  data-substance-set={initialSubstanceSet}
>
  <div class="shadow-left" aria-hidden="true" data-table-shadow></div>
  <div class="table-head-stick">
    <button
      class="corner search-trigger"
      type="button"
      aria-label="Search substances and combinations"
      title="Search substances and combinations"
      data-search-trigger
    >
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path
          d="M10.75 4a6.75 6.75 0 1 0 4.16 12.06l3.52 3.51 1.41-1.41-3.51-3.52A6.75 6.75 0 0 0 10.75 4Zm0 2a4.75 4.75 0 1 1 0 9.5 4.75 4.75 0 0 1 0-9.5Z"
        ></path>
      </svg>
      <span class="search-trigger-label">Search</span>
      <kbd class="search-trigger-kbd" aria-hidden="true">
        <span class="search-trigger-kbd__mac">⌘F</span>
        <span class="search-trigger-kbd__win">Ctrl+F</span>
      </kbd>
    </button>
    <div class="table-head-scroll" data-head-scroll>
      <table class="combos-table combos-table--head" role="presentation">
        <thead>
          <tr>
            {
              columns.map((col) => (
                <th
                  scope="col"
                  class={`col-head group-${col.group} category-surface cell-text ${getLabelSizeClass(col.label)}`}
                  data-substance-slug={col.slug}
                  data-col-key={col.key}
                  data-col-group={col.group}
                  data-col-common={String(isCommonSubstance(col.key))}
                  data-hover-cell
                >
                  <a
                    href={`/${col.slug}`}
                    class="label-link col-link"
                    data-substance={`/${col.slug}`}
                  >
                    <span class="label-text">{col.label}</span>
                  </a>
                </th>
              ))
            }
          </tr>
        </thead>
      </table>
    </div>
  </div>

  <div class="table-scroll" role="region" tabindex="0" data-body-scroll>
    <table class="combos-table combos-table--body" role="grid">
      <tbody>
        {
          columns.map((row) => (
            <tr data-row-substance={row.key} data-row-common={String(isCommonSubstance(row.key))}>
              <th
                scope="row"
                class={`row-head group-${row.group} category-surface cell-text ${getLabelSizeClass(row.label)}`}
                data-substance-slug={row.slug}
                data-row-key={row.key}
                data-row-group={row.group}
                data-row-common={String(isCommonSubstance(row.key))}
                data-hover-cell
              >
                <a
                  href={`/${row.slug}`}
                  class="label-link row-link"
                  data-substance={`/${row.slug}`}
                >
                  <span class="label-text">{row.label}</span>
                </a>
              </th>
              {columns.map((col) =>
                row.key === col.key ? (
                  <th
                    scope="col"
                    class={`diag group-${row.group} category-surface cell-text ${getLabelSizeClass(row.label)}`}
                    data-row-key={row.key}
                    data-col-key={col.key}
                    data-row-group={row.group}
                    data-col-group={col.group}
                    data-row-common={String(isCommonSubstance(row.key))}
                    data-col-common={String(isCommonSubstance(col.key))}
                    data-hover-cell
                  >
                    <a
                      href={`/${row.slug}`}
                      class="label-link diag-link"
                      data-substance={`/${row.slug}`}
                    >
                      <span class="label-text">{row.label}</span>
                    </a>
                  </th>
                ) : (
                  <td
                    class="cell-td"
                    data-row-key={row.key}
                    data-col-key={col.key}
                    data-row-group={row.group}
                    data-col-group={col.group}
                    data-row-common={String(isCommonSubstance(row.key))}
                    data-col-common={String(isCommonSubstance(col.key))}
                    data-hover-cell
                  >
                    <ComboCell
                      rowKey={row.key}
                      colKey={col.key}
                      rowLabel={row.label}
                      colLabel={col.label}
                      comboRoute={comboRoute}
                    />
                  </td>
                )
              )}
            </tr>
          ))
        }
      </tbody>
    </table>
  </div>
</section>

<CategoryNav initialSubstanceSet={initialSubstanceSet} />

<script>
  import { initSearchPalette } from "../scripts/search-palette";

  initSearchPalette();
</script>

<script is:inline>
  (() => {
    const init = () => {
      document.querySelectorAll(".table-wrap").forEach((wrap) => {
        if (!(wrap instanceof HTMLElement) || wrap.dataset.shadowInit === "true") {
          return;
        }

        const bodyScroll = wrap.querySelector("[data-body-scroll]");
        const headScroll = wrap.querySelector("[data-head-scroll]");
        if (!(bodyScroll instanceof HTMLElement) || !(headScroll instanceof HTMLElement)) {
          return;
        }
        const hoverCells = Array.from(
          wrap.querySelectorAll("[data-hover-cell]")
        ).filter((cell) => cell instanceof HTMLElement);

        const update = () => {
          headScroll.scrollLeft = bodyScroll.scrollLeft;
          wrap.dataset.scrolledX = bodyScroll.scrollLeft > 2 ? "true" : "false";
          wrap.dataset.scrolledY =
            wrap.getBoundingClientRect().top < 0 ? "true" : "false";
        };

        const categoryNav = wrap.nextElementSibling;
        const categoryControls =
          categoryNav instanceof HTMLElement
            ? Array.from(categoryNav.querySelectorAll("[data-category]")).filter(
                (control) => control instanceof HTMLElement
              )
            : [];

        const categoryCheckboxes =
          categoryNav instanceof HTMLElement
            ? Array.from(categoryNav.querySelectorAll("[data-category-checkbox]")).filter(
                (cb) => cb instanceof HTMLInputElement
              )
            : [];

        const clearHover = () => {
          wrap.dataset.hovering = "false";
          hoverCells.forEach((cell) => {
            cell.classList.remove("is-highlight", "is-muted");
          });
        };

        const setHover = (target) => {
          if (wrap.dataset.hoverEnabled === "false") return;
          if (!(target instanceof Element)) return;
          const active = target.closest("[data-hover-cell]");
          if (!(active instanceof HTMLElement)) {
            clearHover();
            return;
          }

          const row = active.dataset.rowKey;
          const col = active.dataset.colKey;
          wrap.dataset.hovering = "true";

          hoverCells.forEach((cell) => {
            const rowMatches = row && cell.dataset.rowKey === row;
            const colMatches = col && cell.dataset.colKey === col;
            const highlighted = Boolean(rowMatches || colMatches);
            cell.classList.toggle("is-highlight", highlighted);
            cell.classList.toggle("is-muted", !highlighted);
          });
        };

        const setCategoryHover = (category) => {
          if (wrap.dataset.hoverEnabled !== "false") {
            wrap.dataset.hovering = "true";

            hoverCells.forEach((cell) => {
              const highlighted =
                cell.dataset.rowGroup === category || cell.dataset.colGroup === category;
              cell.classList.toggle("is-highlight", highlighted);
              cell.classList.toggle("is-muted", !highlighted);
            });
          }

          scrollToCategory(category);
        };

        const scrollToCategory = (category) => {
          const col = wrap.querySelector(`[data-col-group="${category}"]`);
          const row = wrap.querySelector(`[data-row-group="${category}"]`);
          if (col instanceof HTMLElement) {
            bodyScroll.scrollTo({
              left: Math.max(0, col.offsetLeft - bodyScroll.clientWidth * 0.08),
              behavior: "smooth",
            });
          }

          if (row instanceof HTMLElement) {
            const stickyOffset =
              wrap.querySelector(".table-head-stick") instanceof HTMLElement
                ? wrap.querySelector(".table-head-stick").getBoundingClientRect().height
                : 0;
            window.scrollTo({
              top: window.scrollY + row.getBoundingClientRect().top - stickyOffset - 4,
              behavior: "smooth",
            });
          }
        };

        const viewButtons =
          categoryNav instanceof HTMLElement
            ? Array.from(categoryNav.querySelectorAll("[data-view]")).filter(
                (b) => b instanceof HTMLButtonElement
              )
            : [];

        const medicationToggle =
          categoryNav instanceof HTMLElement
            ? categoryNav.querySelector("[data-medication-toggle]")
            : null;

        const highlightToggle =
          categoryNav instanceof HTMLElement
            ? categoryNav.querySelector("[data-highlight-toggle]")
            : null;

        const setView = (view) => {
          const substanceSet = view === "full" ? "full" : "common";
          wrap.dataset.substanceSet = substanceSet;
          viewButtons.forEach((btn) => {
            const active = btn.dataset.view === substanceSet;
            btn.classList.toggle("is-active", active);
            btn.setAttribute("aria-pressed", active ? "true" : "false");
          });
          bodyScroll.scrollLeft = 0;
          update();
        };

        const setMedication = (visible) => {
          wrap.dataset.medication = visible ? "true" : "false";
        };

        const setHighlightEnabled = (enabled) => {
          wrap.dataset.hoverEnabled = enabled ? "true" : "false";
          if (!enabled) clearHover();
        };

        const updateCategoryVisibility = () => {
          const hiddenCategories = new Set();
          categoryCheckboxes.forEach((cb) => {
            if (!cb.checked) {
              hiddenCategories.add(cb.dataset.categoryCheckbox);
            }
          });
          wrap.dataset.hiddenCategories = Array.from(hiddenCategories).join(",");
          hoverCells.forEach((cell) => {
            const rowGroup = cell.dataset.rowGroup;
            const colGroup = cell.dataset.colGroup;
            const hidden = hiddenCategories.has(rowGroup) || hiddenCategories.has(colGroup);
            cell.classList.toggle("is-category-hidden", hidden);
          });
          const rows = wrap.querySelectorAll("[data-row-substance]");
          rows.forEach((row) => {
            if (!(row instanceof HTMLElement)) return;
            const rowHead = row.querySelector("[data-row-group]");
            if (!(rowHead instanceof HTMLElement)) return;
            const rowGroup = rowHead.dataset.rowGroup;
            const hidden = hiddenCategories.has(rowGroup);
            row.classList.toggle("is-row-hidden", hidden);
          });
          const colHeads = wrap.querySelectorAll("[data-col-key][data-col-group]");
          colHeads.forEach((col) => {
            if (!(col instanceof HTMLElement)) return;
            const colGroup = col.dataset.colGroup;
            const hidden = hiddenCategories.has(colGroup);
            col.classList.toggle("is-col-hidden", hidden);
          });
        };

        wrap.dataset.shadowInit = "true";
        setView(wrap.dataset.substanceSet === "common" ? "common" : "full");
        update();

        bodyScroll.addEventListener("scroll", update, { passive: true });
        wrap.addEventListener("combo-table:viewchange", () => {
          bodyScroll.scrollLeft = 0;
          update();
        });
        wrap.addEventListener("pointerover", (event) => setHover(event.target));
        wrap.addEventListener("pointerleave", clearHover);
        wrap.addEventListener("focusin", (event) => setHover(event.target));
        wrap.addEventListener("focusout", clearHover);

        viewButtons.forEach((btn) => {
          btn.addEventListener("click", () => {
            setView(btn.dataset.view);
          });
        });

        if (medicationToggle instanceof HTMLInputElement) {
          medicationToggle.addEventListener("change", () => {
            setMedication(medicationToggle.checked);
          });
          setMedication(medicationToggle.checked);
        }

        if (highlightToggle instanceof HTMLInputElement) {
          const isTouchDevice = window.matchMedia("(pointer: coarse)").matches;
          if (isTouchDevice) {
            highlightToggle.checked = false;
          }
          highlightToggle.addEventListener("change", () => {
            setHighlightEnabled(highlightToggle.checked);
          });
          setHighlightEnabled(highlightToggle.checked);
        }

        categoryCheckboxes.forEach((cb) => {
          cb.addEventListener("change", updateCategoryVisibility);
        });

        categoryControls.forEach((control) => {
          control.addEventListener("pointerover", () => {
            const checkbox = control.querySelector("[data-category-checkbox]");
            if (checkbox instanceof HTMLInputElement && !checkbox.checked) return;
            setCategoryHover(control.dataset.category);
          });
          control.addEventListener("pointerleave", clearHover);
          control.addEventListener("focus", () => {
            const checkbox = control.querySelector("[data-category-checkbox]");
            if (checkbox instanceof HTMLInputElement && !checkbox.checked) return;
            setCategoryHover(control.dataset.category);
          });
          control.addEventListener("blur", clearHover);
        });
        window.addEventListener("scroll", update, { passive: true });
        window.addEventListener("resize", update, { passive: true });
      });
    };

    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", init, { once: true });
    } else {
      init();
    }
  })();
</script>

<style>
  .table-wrap {
    --cell-gap: 2px;
    --fluid-cell-size: calc((100cqi - 56px) / 27);
    --table-cell-size: max(64px, var(--fluid-cell-size));
    --cell-size: var(--table-cell-size);
    --row-head-size: calc(var(--table-cell-size) + var(--cell-gap));
    --ink: var(--color-ink);
    --exit-duration: 350ms;
    --enter-duration: 0ms;
    width: 100%;
    padding: 0;
    position: relative;
    container-type: inline-size;
  }

  .table-wrap[data-substance-set="common"] {
    --fluid-cell-size: calc((100cqi - 26px) / 12);
    --table-cell-size: max(72px, var(--fluid-cell-size));
  }

  .table-head-stick {
    position: sticky;
    top: 0;
    z-index: 8;
    display: grid;
    grid-template-columns: var(--row-head-size) 1fr;
    align-items: stretch;
    background-color: var(--ink);
  }

  .table-head-stick::after {
    content: "";
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    height: 14px;
    pointer-events: none;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.28), transparent);
    opacity: 0;
    transition: opacity 140ms ease;
  }

  .table-wrap[data-scrolled-y="true"] .table-head-stick::after {
    opacity: 1;
  }

  .table-head-scroll {
    overflow: hidden;
    background-color: var(--ink);
  }

  .shadow-left {
    position: absolute;
    top: calc(var(--table-cell-size) + var(--cell-gap) * 2);
    left: var(--row-head-size);
    height: calc((var(--table-cell-size) + var(--cell-gap)) * 25);
    width: 14px;
    pointer-events: none;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.28), transparent);
    opacity: 0;
    transition: opacity 140ms ease;
    z-index: 7;
  }

  .table-wrap[data-scrolled-x="true"] .shadow-left {
    opacity: 1;
  }

  .table-scroll {
    position: relative;
    width: 100%;
    overflow-x: auto;
    overflow-y: visible;
    overscroll-behavior-x: contain;
    overscroll-behavior-y: auto;
    touch-action: pan-x pan-y;
    background-color: var(--ink);
    -webkit-overflow-scrolling: touch;
  }

  .table-scroll:focus-visible {
    outline: none;
  }

  .combos-table {
    border-collapse: separate;
    border-spacing: var(--cell-gap);
    background-color: var(--ink);
    font-size: clamp(15px, calc(var(--table-cell-size) * 0.2), 28px);
    table-layout: fixed;
    width: max-content;
    min-width: max-content;
  }

  .combos-table th,
  .combos-table td {
    padding: 0;
    margin: 0;
    width: var(--table-cell-size);
    height: var(--table-cell-size);
    min-width: var(--table-cell-size);
    max-width: var(--table-cell-size);
    box-sizing: border-box;
    overflow: hidden;
  }

  .table-wrap[data-substance-set="common"] [data-col-common="false"],
  .table-wrap[data-substance-set="common"] [data-row-common="false"] {
    display: none;
  }

  [data-hover-cell] {
    transition:
      filter var(--exit-duration) ease,
      opacity var(--exit-duration) ease;
    will-change: filter, opacity;
  }

  .table-wrap[data-hovering="true"] [data-hover-cell] {
    transition:
      filter var(--enter-duration) ease,
      opacity var(--enter-duration) ease;
  }

  .table-wrap[data-hovering="true"] [data-hover-cell].is-muted {
    filter: saturate(0.25) brightness(0.75);
    opacity: 0.7;
  }

  .table-wrap[data-hovering="true"] [data-hover-cell].is-highlight {
    filter: saturate(1.12) brightness(1.1);
    opacity: 1;
  }

  .corner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
    background-color: var(--ink);
    width: var(--row-head-size);
    min-width: var(--row-head-size);
    height: 100%;
  }

  .search-trigger {
    flex-direction: column;
    padding: 8px 5px;
    color: var(--color-fg);
    border: 0;
    border-right: 1px solid rgba(255, 255, 255, 0.08);
    cursor: pointer;
  }

  .search-trigger svg {
    width: clamp(19px, calc(var(--table-cell-size) * 0.3), 26px);
    height: clamp(19px, calc(var(--table-cell-size) * 0.3), 26px);
    fill: currentColor;
    opacity: 0.85;
  }

  .search-trigger-label {
    display: block;
    max-width: 100%;
    overflow: hidden;
    color: rgba(255, 255, 255, 0.6);
    font-size: clamp(9px, calc(var(--table-cell-size) * 0.105), 12px);
    font-weight: 700;
    letter-spacing: 0.04em;
    line-height: 1;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .search-trigger-kbd {
    display: block;
    margin-top: 3px;
    padding: 0;
    color: rgba(255, 255, 255, 0.38);
    background: none;
    border: 0;
    font-family: inherit;
    font-size: clamp(8px, calc(var(--table-cell-size) * 0.09), 10px);
    font-weight: 600;
    letter-spacing: 0.02em;
    line-height: 1;
    white-space: nowrap;
  }

  .search-trigger-kbd__win {
    display: none;
  }

  :global(html[data-mod-key="ctrl"]) .search-trigger-kbd__mac {
    display: none;
  }

  :global(html[data-mod-key="ctrl"]) .search-trigger-kbd__win {
    display: inline;
  }

  @media (pointer: coarse) {
    .search-trigger-kbd {
      display: none;
    }
  }

  .search-trigger:hover svg,
  .search-trigger:focus-visible svg {
    opacity: 1;
  }

  :global(html[data-color-scheme="light"]) .search-trigger {
    color: var(--color-fg);
    border-right-color: rgba(23, 24, 19, 0.1);
  }

  :global(html[data-color-scheme="light"]) .search-trigger-label {
    color: var(--color-fg-mute);
  }

  :global(html[data-color-scheme="light"]) .search-trigger-kbd {
    color: rgba(23, 24, 19, 0.42);
  }

  :global(html[data-search-palette-open]) {
    overflow: hidden;
  }

  :global(.search-palette) {
    position: fixed;
    inset: 0;
    z-index: 90;
    pointer-events: none;
    opacity: 0;
    transition: opacity 120ms ease;
    --search-surface-border: rgba(255, 255, 255, 0.1);
    --search-divider: rgba(255, 255, 255, 0.08);
    --search-hover: rgba(255, 255, 255, 0.06);
    --search-input-border: rgba(255, 255, 255, 0.1);
    --search-input-focus: rgba(255, 255, 255, 0.22);
    --search-match-separator: rgba(255, 255, 255, 0.22);
    --search-shadow: 0 24px 64px rgba(0, 0, 0, 0.5);
    --search-border-strength: 62%;
    --search-border-strength-active: 38%;
  }

  :global(html[data-color-scheme="light"] .search-palette) {
    --search-surface-border: rgba(23, 24, 19, 0.12);
    --search-divider: rgba(23, 24, 19, 0.08);
    --search-hover: rgba(23, 24, 19, 0.04);
    --search-input-border: rgba(23, 24, 19, 0.12);
    --search-input-focus: rgba(23, 24, 19, 0.28);
    --search-match-separator: rgba(23, 24, 19, 0.18);
    --search-shadow: 0 24px 64px rgba(24, 25, 20, 0.14);
    --search-border-strength: 48%;
    --search-border-strength-active: 24%;
  }

  :global(.search-palette[data-open="true"]) {
    pointer-events: auto;
    opacity: 1;
  }

  :global(.search-palette__backdrop) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    padding: 0;
    background: rgba(0, 0, 0, 0.52);
    border: 0;
    cursor: pointer;
  }

  :global(html[data-color-scheme="light"] .search-palette__backdrop) {
    background: rgba(23, 24, 19, 0.36);
  }

  :global(.search-palette__panel) {
    position: absolute;
    top: clamp(36px, 11dvh, 110px);
    left: 50%;
    display: flex;
    flex-direction: column;
    width: min(640px, calc(100vw - 32px));
    max-height: min(560px, calc(100dvh - 48px));
    transform: translateX(-50%);
    color: var(--color-fg);
    background: var(--color-ink-2);
    border: 1px solid var(--search-surface-border);
    border-radius: 12px;
    box-shadow: var(--search-shadow);
    transition: opacity 120ms ease;
  }

  :global(.search-palette__form) {
    position: relative;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    gap: 0;
    padding: 12px 14px;
    border-bottom: 1px solid var(--search-divider);
  }

  :global(.search-palette__search-icon) {
    position: absolute;
    left: 26px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    color: var(--color-fg-mute);
    pointer-events: none;
  }

  :global(.search-palette__search-icon svg) {
    width: 18px;
    height: 18px;
    fill: currentColor;
  }

  :global(.search-palette__input) {
    width: 100%;
    height: 44px;
    padding: 0 14px 0 40px;
    color: var(--color-fg);
    background: var(--color-ink-3);
    border: 1px solid var(--search-input-border);
    border-radius: 8px;
    font: inherit;
    font-size: 16px;
    font-weight: 500;
    outline: none;
  }

  :global(.search-palette__input::placeholder) {
    color: var(--color-fg-mute);
    opacity: 0.7;
  }

  :global(.search-palette__input:focus) {
    border-color: var(--search-input-focus);
  }

  :global(.search-palette__results) {
    flex: 1;
    min-height: 0;
    overflow-y: auto;
    overscroll-behavior: contain;
    padding: 4px 0;
    scroll-padding-block: 4px;
  }

  :global(.search-palette__result) {
    position: relative;
    display: block;
    padding: 10px 16px 10px 20px;
    color: inherit;
    text-decoration: none;
    transition: background 80ms ease;
  }

  :global(.search-palette__result--combo) {
    padding-left: 24px;
  }

  :global(.search-palette__result-borders) {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    display: flex;
    gap: 1px;
    width: 3px;
  }

  :global(.search-palette__result-borders--combo) {
    width: 7px;
  }

  :global(.search-palette__result-border) {
    flex: 1;
    min-width: 3px;
    background: color-mix(in oklab, var(--hue), transparent var(--search-border-strength));
  }

  :global(.search-palette__result:hover),
  :global(.search-palette__result.is-active) {
    background: var(--search-hover);
  }

  :global(.search-palette__result.is-active .search-palette__result-border) {
    background: color-mix(in oklab, var(--hue), transparent var(--search-border-strength-active));
  }

  :global(.search-palette__result-title) {
    display: flex;
    align-items: baseline;
    gap: 8px;
    min-width: 0;
    overflow: hidden;
    line-height: 1.25;
  }

  :global(.search-palette__result-name) {
    flex: 0 1 auto;
    overflow: hidden;
    font-size: 15px;
    font-weight: 600;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.search-palette__result-match) {
    flex: 0 0 auto;
    color: var(--color-fg-mute);
    font-size: 13px;
    font-weight: 500;
    white-space: nowrap;
  }

  :global(.search-palette__result-match::before) {
    content: "·";
    margin-right: 8px;
    color: var(--search-match-separator);
  }

  :global(.search-palette__result-subtitle) {
    display: block;
    margin-top: 2px;
    overflow: hidden;
    color: var(--color-fg-mute);
    font-size: 13px;
    font-weight: 450;
    line-height: 1.3;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.search-palette__empty) {
    margin: 0;
    padding: 32px 16px;
    color: var(--color-fg-mute);
    font-size: 14px;
    line-height: 1.5;
    text-align: center;
  }

  :global(.search-palette__footer) {
    flex-shrink: 0;
    display: flex;
    gap: 16px;
    justify-content: center;
    padding: 10px 16px;
    color: var(--color-fg-mute);
    border-top: 1px solid var(--search-divider);
    font-size: 11px;
    font-weight: 500;
    line-height: 1.2;
  }

  .table-wrap[data-medication="false"] tr[data-row-substance="maois"],
  .table-wrap[data-medication="false"] tr[data-row-substance="ssris"] {
    display: none;
  }

  .table-wrap[data-medication="false"] th[data-col-key="maois"],
  .table-wrap[data-medication="false"] th[data-col-key="ssris"],
  .table-wrap[data-medication="false"] td[data-col-key="maois"],
  .table-wrap[data-medication="false"] td[data-col-key="ssris"] {
    display: none;
  }

  .is-row-hidden {
    display: none !important;
  }

  .is-col-hidden {
    display: none !important;
  }

  .col-head {
    height: var(--table-cell-size);
    min-height: var(--table-cell-size);
    width: var(--table-cell-size);
    min-width: var(--table-cell-size);
    padding: 0;
  }

  .label-link {
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
    padding: clamp(6px, calc(var(--table-cell-size) * 0.09), 14px);
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    box-sizing: border-box;
  }

  .col-head .label-text,
  .row-head .label-text {
    display: inline-block;
    font-weight: 600;
    font-size: clamp(13px, calc(var(--table-cell-size) * 0.13), 18px);
    letter-spacing: 0.02em;
    line-height: 1.08;
    text-align: left;
    white-space: normal;
    overflow-wrap: break-word;
    max-width: 100%;
  }

  .col-head .label-link,
  .row-head .label-link {
    justify-content: flex-start;
  }

  .row-head {
    position: sticky;
    left: 0;
    z-index: 3;
    width: var(--table-cell-size);
    min-width: var(--table-cell-size);
    background-clip: padding-box;
    box-shadow: inset -1px 0 0 rgba(255, 255, 255, 0.04);
  }

  .row-head .row-link {
    font-size: inherit;
  }

  .row-head .row-link:hover,
  .col-head .label-link:hover {
    filter: brightness(1.2);
  }

  .diag .diag-link {
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
    padding: 0;
    text-decoration: none;
    color: inherit;
  }

  .diag .label-text {
    display: inline-block;
    transform: rotate(-45deg);
    font-weight: 600;
    font-size: clamp(11px, calc(var(--table-cell-size) * 0.115), 16px);
    white-space: nowrap;
    line-height: 1;
    opacity: 0.9;
  }

  .label-xs .label-text {
    font-size: clamp(10.5px, calc(var(--table-cell-size) * 0.09), 13.5px);
    letter-spacing: 0;
  }

  .label-sm .label-text {
    font-size: clamp(12px, calc(var(--table-cell-size) * 0.105), 15px);
  }

  .label-md .label-text {
    font-size: clamp(13px, calc(var(--table-cell-size) * 0.12), 16.5px);
  }

  .cell-td {
    padding: 0;
    background-color: var(--ink);
  }

  .combos-table td.cell-td {
    position: relative;
    overflow: visible;
  }

  .combos-table td.cell-td:hover,
  .combos-table td.cell-td:focus-within {
    z-index: 6;
  }

  @media (max-width: 640px) {
    .table-wrap {
      --fluid-cell-size: 54px;
      --table-cell-size: 54px;
    }

    .table-wrap[data-substance-set="common"] {
      --fluid-cell-size: 58px;
      --table-cell-size: 58px;
    }

    .combos-table {
      font-size: 17px;
    }

    .label-xs .label-text {
      font-size: 9.5px;
    }

    .label-sm .label-text {
      font-size: 10.5px;
    }

    .label-md .label-text {
      font-size: 11.5px;
    }

    .search-trigger {
      padding: 7px 4px;
    }

    .search-trigger-label {
      font-size: 8px;
    }

    .search-trigger-kbd {
      margin-top: 2px;
      font-size: 7px;
    }

    :global(.search-palette__panel) {
      top: 0;
      left: 0;
      width: 100vw;
      height: 100dvh;
      max-height: 100dvh;
      transform: none;
      border: none;
      border-radius: 0;
      box-shadow: none;
    }

    :global(.search-palette__form) {
      position: sticky;
      top: 0;
      z-index: 1;
      padding: 12px 14px;
      background: var(--color-ink-2);
    }

    :global(.search-palette__input) {
      height: 48px;
      font-size: 16px;
    }

    :global(.search-palette__footer) {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-hover-cell] {
      transition: none !important;
    }

    .search-trigger,
    :global(.search-palette),
    :global(.search-palette__result) {
      transition: none !important;
    }
  }
</style>
